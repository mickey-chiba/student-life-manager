<div class="form-actions">
  <% if (editItem) { %><a class="button ghost" href="<%= cancel %>">キャンセル</a><% } %>
  <button class="button primary" type="submit"><%= editItem ? "変更を保存" : "登録する" %></button>
</div>
